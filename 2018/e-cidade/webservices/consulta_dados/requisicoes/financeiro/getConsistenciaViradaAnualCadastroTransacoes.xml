<?xml version="1.0" ?>
<consulta>

  <campos>

    <campo campo="db_config.codigo"  label="instit"></campo>
    <campo campo="(SELECT count(*)
                     FROM contrans a
                    WHERE c45_anousu = 2014
                      AND a.c45_instit = db_config.codigo)" label="qtd_2014"></campo>
    <campo campo="(SELECT count(*)
                     FROM contrans a
                    WHERE c45_anousu = 2015
                      AND a.c45_instit = db_config.codigo)" label="qtd_2015"></campo>
  </campos>

  <from tabela="db_config"></from>

  <juncoes></juncoes>
  <filtros></filtros>

  <agrupamento></agrupamento>
  <ordenacao campos="codigo"></ordenacao>

</consulta>

